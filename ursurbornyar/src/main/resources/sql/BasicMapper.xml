<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.my.ursurbornyar.mapper.BasicMapper">

	<insert id = "insertPlace" parameterType="com.my.ursurbornyar.vo.Place">
		INSERT INTO PLACE_TB (
			ID,
			NAME,
			WGS_X,
			WGS_Y,
			GRS_X,
			GRS_Y
		)
		VALUES (
			#{place.id},
			#{place.name},
			#{place.coor.x},
			#{place.coor.y},
			null,
			null
		)
	</insert>
	
	<insert id = "insertPointSet" parameterType="com.my.ursurbornyar.vo.Path">
		INSERT INTO POINT_SET_TB (
			ID,
			START_POINT_ID,
			END_POINT_ID
		)
		VALUES (
			#{path.id},
			#{path.start_point_id},
			#{path.end_point_id}
		)
	</insert>
	
	<select id = "checkDuplicatedPlace" parameterType="com.my.ursurbornyar.vo.Place" resultType = "Integer">
		SELECT
			COUNT(*)
		FROM
			PLACE_TB
		WHERE
			 WGS_X = #{place.coor.x} AND WGS_Y = #{place.coor.y}
		OR
			 NAME = #{place.name}
	</select>
	
	<select id = "countPlace" resultType = "Integer">
		SELECT
			COUNT(*)
		FROM
			PLACE_TB
	</select>
	
	<select id = "countPointSet" resultType = "Integer">
		SELECT
			COUNT(*)
		FROM
			POINT_SET_TB
	</select>
	
	<select id = "check" resultType = "Integer">
		SELECT 
			COUNT(*)
		FROM
			PATH_TB
	</select>
</mapper>
