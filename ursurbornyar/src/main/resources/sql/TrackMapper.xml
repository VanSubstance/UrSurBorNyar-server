<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.my.ursurbornyar.mapper.TrackMapper">
			
	<select id = "countTrack" resultType = "Integer">
		SELECT
			COUNT(*)
		FROM
			TRACK_TB
	</select>
	
	<insert id = "insertTrack" parameterType = "com.my.ursurbornyar.vo.Track">
		INSERT INTO TRACK_TB (
			DISTANCE,
			TIME,
			POINT_SET_ID
		)
		VALUES (
			#{track.distance},
			#{track.time},
			#{track.point_set_id}
		)
	</insert>
	
	<select id = "checkDuplicatedTrack" parameterType="com.my.ursurbornyar.vo.Track" resultType = "Integer">
		SELECT
			COUNT(*)
		FROM
			TRACK_TB
		WHERE
			 DISTANCE = #{track.distance} AND TIME = #{track.time} AND POINT_SET_ID = #{track.point_set_id}
	</select>
	
	<select id = "getTrackID" parameterType = "com.my.ursurbornyar.vo.Track" resultType = "Integer">
		SELECT
			ID
		FROM
			TRACK_TB
		WHERE
			DISTANCE = #{track.distance} AND TIME = #{track.time} AND POINT_SET_ID = #{track.point_set_id}
	</select>
</mapper>